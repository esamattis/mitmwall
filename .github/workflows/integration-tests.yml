name: Integration tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  integration-tests:
    runs-on: ubuntu-24.04
    steps:
      - name: Placeholder
        run: true
